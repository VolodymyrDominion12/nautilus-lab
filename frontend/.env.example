VITE_API_URL=http://localhost:8000
# Behind one reverse proxy that serves the dashboard and /api (deploy/Caddyfile):
# VITE_API_URL=same-origin
# Must match API_TOKEN in the backend .env when that is set; empty = origin check only.
VITE_API_TOKEN=
