# 24. Paper-трейдинг: як запускати, що виміряно, чого бракує

> **Призначення.** Це документ про **paper-сесії** — режим, у якому робот іде вперед
> замороженою конфігурацією, а кожна угода, позиція й комісія лягають у журнал.
> Він описує, що саме зроблено в коді (і чим це підтверджено), які роботи проходять
> paper-сесію, які числа вони дають на реальному каталозі, і **чому тікові дані поки
> не є основою дослідження** — з вимірами, а не з міркувань.
>
> Числа нижче отримані прогонами 2026-09-23 на каталозі
> `catalog/` (ETH/USDT, 1h, 2024-01-01…2026-09-18, 23 808 барів). Вони не вигадані:
> кожне або походить з виводу CLI, або позначене як «не виміряно».

---

## 1. Що таке paper-сесія тут — і чим вона **не** є

| | Research (walk-forward) | **Paper-сесія** | Live |
|---|---|---|---|
| Хто обирає параметри | сітка/Optuna на IS | ніхто (або `--select-on-is` — на історії **до** вікна) | ніхто |
| Що на виході | OOS-число | журнал: філи, позиції, комісії, PnL, mark-to-market | — |
| Куди йдуть ордери | у симулятор | у симулятор | **заборонено кодом** |
| Дані | каталог | каталог / синтетика / **живий WS** | — |

Paper-сесія **не** є out-of-sample звітом: у ній нічого не підбирається на OOS, бо
нічого не підбирається взагалі. Це репетиція перед реальним майданчиком: перевірка,
що робот робить те, що обіцяє, і що його витрати не з'їдають результат.

Ключове архітектурне рішення: **paper використовує той самий рушій, що й бектест**
(`NautilusResearchBacktest`), з тими самими комісіями, затримкою 50 мс і моделлю
філів. Другий, написаний вручну «паперовий» матчер розійшовся б із числами
досліджень, і розходження було б невидимим рівно доти, доки не стало б важливим.

Жоден ордер не йде на біржу не за домовленістю, а за кодом: `RunPaperSession.execute()`
починається з `require_simulated_mode(request.mode)` (`application/risk.py`), і
`TradingMode.LIVE` падає там з `LiveTradingDisabledError`. Шляху виконання, крім
симулятора, у лабораторії немає — тому `lab live` і не «полагодити».

---

## 2. Як запускати

```bash
# Сесія на реальній історії: останні 1500 закритих барів, повний журнал угод
.venv/bin/lab paper --robot regime --bars 1500 --journal

# Параметри обираються ЛИШЕ на історії, що передує вікну сесії (+ ембарго)
.venv/bin/lab paper --robot vpin_momentum --bars 400 --select-on-is

# Живий режим: розігрів з каталогу + закриті бари з публічного WS Binance
.venv/bin/lab paper --robot regime --bars 300 --source live --live-bars 5

# Синтетика — тільки для тестів, її числа нічого не означають
.venv/bin/lab paper --robot ema --bars 300 --source synthetic
```

Типові значення (з `lab paper --help`): `--bars 2000`, `--robot regime`,
`--source catalog`, `--live-bars 5`, `--live-timeout 900` (секунд),
`--embargo-bars` — порожньо, тобто береться `Settings.embargo_bars` (10); решта
прапорців (`--select-on-is`, `--tick-vpin`, `--hawkes`, `--journal`) типово вимкнені.
`--robot` приймає лише вісім назв із `PAPER_SUPPORTED_ROBOTS` (§3) — жодного іншого.

Що друкує (реальний прогін):

```
paper session (no exchange submission) robot=regime instrument=ETH/USDT.SIM mode=paper source=catalog
bars=1500 window=[2026-07-18T12:59:59.999000+00:00, 2026-09-18T23:59:59.999000+00:00]
starting=100000 ending_balance=95162.43356245 net_pnl_marked=-3318.52606755 return_marked=-3.32%
fills=95 positions=48 fees=4194.53183755 traded_notional=4194531.83755 max_dd=0.0683
open_position side=LONG qty=14.647 entry=2508.43 mark=2612.14 unrealized=1519.04
```

Дві окремі величини — навмисно. `ending_balance` це рахунок **після реалізованих**
філів; `net_pnl_marked` додає переоцінку відкритої позиції за останнім закриттям.
Сесія, що закінчується з відкритою позицією, не є «плоскою», і показувати лише
баланс означало б приховати саме той ризик, який ще живий.

Кожна сесія дописується в `reports/paper/sessions.jsonl` (`--journal`): вікно, кожен
філ, кожна позиція, комісії, спрацювання запобіжників. Append-only — старий рядок
не переписується ніколи.

---

## 3. Які роботи проходять paper-сесію

`PAPER_SUPPORTED_ROBOTS` **дорівнює** `BACKTEST_WIRED_ROBOTS` (звірено тестом
`test_paper_supported_set_is_exactly_the_wired_set`): обіцянка CLI не може розійтися
з тим, що рушій уміє зібрати. Робот без адаптера падає явно, а не підміняється іншим.

Прогін 2026-09-23, `--bars 1500` (2026-07-18…2026-09-18), `reports/paper/sessions.jsonl`:

| Робот | fills | positions | `return_marked` | комісії | Що це означає |
|---|---:|---:|---:|---:|---|
| `regime` | 95 | 48 | −3.32% | 4 194.53 | працює, але комісії більші за сам збиток |
| `ema` | 67 | 34 | −24.77% | 3 087.63 | відкритий шорт проти руху; базовий робот, не робочий |
| `pairs` | 148 | 74 | −5.41% | 4 787.87 | 148 філів на 1500 барів — оборот вбиває |
| `ml_obi` | 63 | 32 | −3.36% | 1 952.53 | **працює лише після виправлення двох дефектів** (§6) |
| `meta_label` | 15 | 8 | **+6.50%** | 683.20 | потрібен `META_LABEL_MODEL_PATH` (модель навчена офлайн) |
| `adaptive_ema` | 28 | 12 | +2.56% | 1 001.59 | `status: rejected` у спеці — підтверджене відхилення, не кандидат |
| `vpin_momentum` | **0** | 0 | 0.00% | 0 | стоїть осторонь: поріг токсичності 0.7 на 1h недосяжний (§5) |
| `formulaic_lgbm` | **0** | 0 | 0.00% | 0 | без натренованої моделі класифікатор не перетинає поріг |

Шість роботів із таблиці реально торгують у paper: `regime`, `ema`, `pairs`,
`ml_obi`, `meta_label`, `adaptive_ema`. Два (`vpin_momentum`, `formulaic_lgbm`)
**запускаються й чесно не торгують** — це не зламаний режим, а незакрита гіпотеза;
різницю описано в §5.

Перевірка «робот справді торгує саме цей» закріплена тестом: `PAPER_SUPPORTED_ROBOTS`
мусить дорівнювати `BACKTEST_WIRED_ROBOTS`, тож CLI не може обіцяти робота, якого
рушій не збирає, а робот без адаптера падає явно замість тихої підміни.

> **Походження чисел у таблиці (перевірено проти `reports/paper/sessions.jsonl`).**
> П'ять рядків звірено з записами журналу від 2026-09-23 з `--bars 1500`: `regime`
> (95 філів / 4 194.53), `ema` (67 / 3 087.63), `pairs` (148 / 4 787.87), `ml_obi`
> (63 / 1 952.53), `meta_label` (15 / 683.20) — збігаються точно.
> **Два рядки журнал не підтверджує, і це варто знати, перш ніж на них спиратися:**
> для `adaptive_ema` запис `--bars 1500` дає `fills=94, positions=47, fees=4 078.58`
> (а не 28 / 12 / 1 001.59), а комбінація `28 / 1 058.19` у журналі належить запису
> `ema --bars 23808`; для `formulaic_lgbm` запису з `--bars 1500` немає взагалі —
> єдиний запис це `--bars 300` із 40 філами, а `vpin_momentum` у журналі відсутній.
> Висновок таблиці від цього не змінюється (`adaptive_ema` справді торгує, а
> `vpin_momentum`/`formulaic_lgbm` справді не торгують), але конкретні числа в цих
> трьох рядках — з прогонів, яких у репозиторії немає. Щоб отримати відтворювані
> числа, повтори прогін із `--journal`: рядок допишеться в той самий файл.

### 3.1. Живий paper — окремий, коротший список роботів

`--source live` у CLI не додає нових роботів, а навпаки — забирає двох: `_run_paper`
(`interfaces/cli.py`) відмовляє явно для `pairs` (дві ноги) і `ml_obi` (потрібен живий
L2-потік, якого в сесії немає). Решта шість із `PAPER_SUPPORTED_ROBOTS` доступні.

Живий термінал у дашборд-API (`api/paper_streamer.py`) має власний, ще коротший
`LIVE_PAPER_ROBOTS`: `regime`, `ema`, `adaptive_ema`, `vpin_momentum`,
`formulaic_lgbm` і бенчмарк `hold` — без `pairs`, `meta_label` і `ml_obi`. Робот поза
цим набором падає з `ValueError`, а не підміняється `regime`. Перед торгівлею робот
розігрівається на `WARMUP_BARS = 300` закритих барах з історії (без торгівлі), а кілька
сесій на одній парі символ+інтервал читають **один** WS-фід (`api/market_feed.py`), а не
по сокету кожна. Журнал термінала — append-only JSONL
(`infrastructure/live_paper_journal.py`): по файлу на сесію; порожній
`LIVE_PAPER_SESSIONS_DIR` означає теку `sessions/` поряд із `LIVE_PAPER_JOURNAL`, а
порожні обидва — журнал лише в пам'яті. Сесія без `session_stop` відновлюється після
рестарту. Набір сесій описує портфель
`deploy/paper_portfolio.yaml` (`LIVE_PAPER_PORTFOLIO`), поруч — `LIVE_PAPER_AUTOSTART`,
`LIVE_PAPER_SYMBOL`, `LIVE_PAPER_INTERVAL`, `LIVE_PAPER_ROBOT`, `LIVE_PAPER_MAX_SESSIONS`,
`LIVE_PAPER_MAX_FEEDS`.

---

## 4. Чесний OOS-аудит тих самих роботів (folds=4)

`lab research --robot <X> --folds 4` на тому самому каталозі, 2026-09-23
(логи: `reports/audit/*.txt`). OOS-вікна: 2025-11-25 … 2026-09-18. У таблиці — шість
роботів із восьми з §3.

| Робот | OOS mean | медіана | найгірший | baseline buy&hold | вердикт | breakeven vs сплачені 10 б.п. |
|---|---:|---:|---:|---:|---|---|
| `regime` | +0.20% | +0.68% | −5.84% | **+1.67%** | не обганяє | 7.96 б.п. — **нижче** сплачених |
| `ema` | −5.60% | −6.64% | −8.36% | +1.67% | не обганяє | −20.33 б.п. |
| `pairs` | −6.09% | −6.11% | −7.28% | +1.67% | не обганяє | −15.62 б.п. |
| `vpin_momentum` | 0.00% | 0.00% | 0.00% | +1.67% | не торгує (0 філів) | n/a |
| `formulaic_lgbm` | 0.00% | 0.00% | 0.00% | +1.67% | не торгує (0 філів) | n/a |
| `meta_label` | −1.14% | −2.44% | −3.60% | +1.67% | не обганяє | −6.04 б.п. |

`meta_label` виміряний із натренованою моделлю (`META_LABEL_MODEL_PATH=models/meta_label.txt`,
108 OOS-філів). `ml_obi` у цій таблиці відсутній свідомо: його історія книги — одна
доба, і жодна нарізка на 4 фолди на ній не має сенсу.

**Жоден робот не має статусу `validated`.** `regime` — єдиний, хто тримає плюс на
OOS, і він усе одно відстає від простого утримання ETH, а його поріг беззбитковості
(7.96 б.п.) **нижчий** за реально сплачені 10 б.п. Це означає: математично він
збитковий за поточних комісій, навіть коли «показує плюс» до витрат.

Окремо про `docs/21` §1.2: таблиця там (buy&hold +2.50%) отримана на інших вікнах і
іншому стані каталогу. Порівнювати її з таблицею вище не можна — це різні періоди.

### 4.1. Аудит перенавчання (PBO/CSCV + DSR), 8 блоків

`lab research --robot <X> --pbo` на тому самому каталозі (логи: `reports/audit/*_pbo.txt`).
Чекліст `docs/21` §10 вимагає **PBO < 0.25** і **DSR p < 0.05**:

| Робот | Конфігурацій | PBO | DSR p | Sharpe переможця | Вердикт |
|---|---:|---:|---:|---:|---|
| `regime` | 6 | **0.043** | 1.4e-6 | **−0.173** | PBO проходить; DSR не є свідченням переваги (див. нижче) |
| `ema` | 4 | 0.271 | 0.174 | +0.230 | обидва не проходять |
| `pairs` | 3 | **1.000** | 0.003 | **−1.055** | вибір не кращий за монетку; Sharpe від'ємний |
| `meta_label` | 3 | **0.971** | 0.762 | +0.590 | не проходить |
| `vpin_momentum` | 3 | undefined | undefined (`zero variance`) | — | не торгує, аудиту немає на чому стояти |
| `ml_obi` | — | не виміряно | — | — | історія книги — одна доба |

Два рядки тут варті окремого слова, бо їх легко прочитати неправильно:

- **`regime`: PBO=0.043 — це добре, і цього мало.** Низький PBO каже лише, що
  in-sample переможець не розсипається на інших блоках. DSR переможця при цьому
  **від'ємний** (−0.173), тобто «переможець» сітки гірший за середній випадковий
  вибір. Це той самий випадок, який `docs/22` §7.2 уже описував: два чесні аудити
  дають різні відповіді, і саме тому чекліст вимагає **всіх** критеріїв.
- **`pairs` і `meta_label`: PBO ≈ 1** означає протилежне до «поганого результату» —
  ранжування конфігурацій настільки нестабільне, що вибір кращої на IS не несе
  інформації про OOS. Для `meta_label` це особливо повчально: у paper-сесії він дав
  **+6.50%** (§3), а аудит перенавчання каже, що вибір його порогу — монетка. Один
  гарний прогін і доказ переваги — різні речі.

---

## 5. Тікові дані: виміряно, а не припущено

Питання «чи краще тестувати на тіках» має дві різні відповіді для двох різних задач.

### 5.1. Що дає тік-дані **в коді** (уже підключено)

- `lab ingest --trades` пише агреговані угоди (`api/v3/aggTrades`) у
  `catalog/data/agg_trade/<SYMBOL>/<YYYY-MM-DD>.parquet`.
- `TickVpin` і `ExponentialHawkes` живляться цим потоком (`on_trade_tick`) і вмикаються
  прапорцями `--tick-vpin` / `--hawkes`. Обидва фільтри — входи regime-роутера, тож
  підключені не для всіх роботів: `--tick-vpin` — для `regime`, `meta_label`,
  `vpin_momentum` (`TICK_VPIN_ROBOTS`), `--hawkes` — лише для `regime` і `meta_label`
  (`HAWKES_ROBOTS`). На інших роботах прапорець приймається, але рішень не змінює
  (`tick_filters_supported` у `domain/regime.py` описує, чому це саме так).
- Рушій приймає тіки як `TradeTick` поруч із барами.

### 5.2. Стеля швидкості заливки (виміряно 2026-09-23)

Тіки технічно доступні, але публічний REST віддає їх надто дрібними порціями.

| Вимір | Результат |
|---|---|
| Одна сторінка `aggTrades` | 1000 угод |
| Скільки ринкового часу покриває сторінка | **~120 с** (історичне вікно 2026-09-17) … ~205 с (свіжі угоди) |
| Отже, одна доба ETHUSDT | **≈ 720 сторінок** |
| Один рік ETHUSDT | ≈ 263 000 сторінок |
| Типова сторінка | 0.7 с; інколи 4.7 с |
| Сторінки, що **зависають і не відповідають** | приблизно 1 з 3 (виміряно: 1/8, далі жорсткий таймаут 10 с) |

Три прогріни, які це показали (усі — реальні, не оцінки):

| Версія заливки | Результат |
|---|---|
| усе вікно в пам'ять (4 доби) | **50 хв — жодного дня на диску** |
| доба за добою (1 доба) | **30 хв — жодного файлу** |
| годинними зрізами (2 год) | **10 хв — жодного файлу** |
| годинними зрізами (30 хв = один зріз) | **7 хв — жодного файлу, процес ще всередині `fetch`** |

Останні два рядки — важливі й неприємні: **навіть один зріз не завершився живо**.
Отже, що саме доведено, а що ні:

- **Доведено тестами** (фейковий фід, без мережі): зріз пишеться на диск **до** того,
  як завантажується наступний; тихий зріз не зупиняє заливання; порожнє вікно дає
  `CatalogEmptyError`; прогрес-колбек викликається на кожен зріз
  (`tests/unit/test_binance_agg_trades.py`).
- **НЕ доведено живим прогоном**: що зріз реально долітає до диска на справжньому
  API. Жоден із чотирьох живих прогонів не дійшов до запису — тому твердження
  «зріз лягає одразу» тут **не** робиться.

Причина — транспорт, а не логіка: застрягла сокет-операція коштувала 30 с таймауту
**перед** повторною спробою, тож заливання проводило майже весь час в очікуванні
сокетів, які вже не відповідять. Таймаут знижено до 10 с
(`_HTTP_TIMEOUT_SECONDS`, із тестом, який це фіксує) — але це лікує ціну затримки,
а не її частоту. Найімовірніша причина частоти — тротлінг цього IP: паралельно
працюють дашборд-API і кілька моїх проб, усі по тих самих ендпоінтах.

**Висновок для планування:** REST-історія тіків недосяжна, але й не потрібна —
живий потік працює і виміряний.

### 5.2.1. Живий збір: єдиний робочий шлях (виміряно)

`lab ingest --trades --live-ticks MINUTES` слухає публічний
`wss://stream.binance.com:9443/ws/<symbol>@aggTrade` і пише в **той самий** каталог.
Ключів не потрібно, обмеження обов'язкове (дедлайн і/або стеля угод), запис — батчами.

Прогін 2026-09-23, 20 хвилин ETHUSDT:

| Вимір | Результат |
|---|---|
| Угод зібрано | **8 265** |
| Вікно | 12:28:20 → 12:48:17 UTC |
| Батчів запису | 2 (по 5 000) |
| Файл | `catalog/data/agg_trade/ETHUSDT/2026-09-23.parquet` |

Це протиставляється §5.2 буквально: чотири прогони REST не записали **нічого**, а
двадцять хвилин потоку записали 8 265 реальних угод. REST дає історію, яку не
донести; WS дає потік, який накопичується, поки ти чекаєш.

### 5.2.2. VPIN на справжніх тіках: висновок docs/23 уточнюється

`scripts/measure_tick_vpin.py` рахує розподіл VPIN на збереженій серії. На тих
8 265 угодах:

| Кошик | Завершених кошиків | медіана | p90 | максимум | «токсичних» (≥ 0.7) |
|---|---:|---:|---:|---:|---:|
| 1000 (типовий `VPIN_BUCKET_VOLUME`) | **4** | 0.275 | 0.439 | 0.462 | **0 %** |
| 250 | **16** | 0.486 | 0.758 | **0.892** | **25 %** |

Тут важливі дві речі, і друга змінює попередній висновок:

1. **Кошик 1000 завеликий для живого потоку**: 20 хвилин дають лише 4 завершені
   кошики (~12 на годину). Це вимір того, про що `specs/strategies/vpin_momentum.yaml`
   попереджав словами («потребує калібрування під таймфрейм»).
2. **Поріг 0.7 досяжний.** На барах 1h максимум VPIN був 0.694 (docs/23 §6), і з
   цього виплив висновок «поріг недосяжний». На тіках із кошиком 250 максимум
   **0.892**, а 25 % кошиків перетинають 0.7. Тобто твердження треба читати вужче:
   поріг недосяжний **на барах із кошиком 1000**, а не «взагалі».

Що це **не** означає: що `vpin_momentum` треба зараз переналаштувати. Калібрування
кошика — окрема гіпотеза, і перевіряти її треба на IS з новою спекою, а не рухати
пороги після побаченого OOS. Це зафіксований напрямок наступного кроку, а не
дозвіл підкрутити поріг. 20 хвилин потоку — теж не вибірка: 16 кошиків нічого не
доводять, вони лише показують, що механізм не мертвий.

### 5.2.3. A/B у paper-сесії: фільтр підключений, але інертний

Ті самі 200 барів (2026-09-12 05:59 … 2026-09-23 12:59), ті самі 8 265 зібраних тіків
усередині вікна, три прогони:

```bash
.venv/bin/lab paper --robot regime --bars 200              # без фільтра
.venv/bin/lab paper --robot regime --bars 200 --tick-vpin  # тік-рівневий VPIN
.venv/bin/lab paper --robot regime --bars 200 --hawkes     # тік-рівневий Хоукс
```

| Прогін | fills | return_marked | комісії |
|---|---:|---:|---:|
| без фільтра | 10 | +1.92% | 330.87 |
| `--tick-vpin` | 10 | +1.92% | 330.87 |
| `--hawkes` | 10 | +1.92% | 330.87 |

Результати **збігаються до копійки** — і це не «фільтр не підключився». Це
підтвердження виміру §5.2.2 з іншого боку: за кошика 1000 максимум VPIN на цьому
потоці — 0.462, тобто поріг 0.7 не перетинається жодного разу, гейт увесь час
відчинений, і рішення не змінюються. Два незалежні шляхи (скрипт по серії та
прогін рушія) дали ту саму відповідь, і саме цього бракувало раніше: доти жоден
прогін із тіками взагалі не виконувався (дефекти 6–7 у §6).

Тобто готовність фільтра і його корисність — різні речі. Підключено й перевірено
перше; друге впирається в калібрування кошика під таймфрейм, і це наступний крок,
а не привід рухати поріг.

### 5.3. Що з цього випливає

1. **Тіки — не заміна барам, а окремий інструмент.** На 1-годинних барах
   мікроструктурна ознака не рятує альфу: `vpin_momentum` із реальним потоком
   тейкерів має `oos_fills=0`, бо поріг токсичності 0.7 не досягається навіть раз
   (максимум VPIN за 23 808 кошиків — 0.694). Проксі tick-rule давав 99.9%
   «токсичних» кошиків — тобто фільтром не був.
2. **Тіки доречні точково**: кілька діб навколо конкретної події, для `ml_obi`
   (книга ордерів, 100 мс) і для живого paper-режиму, де потік і так приходить по WS.
3. **Пороги, які не досягаються, не можна «підкручувати, щоб заторгувало».**
   `specs/strategies/vpin_momentum.yaml` фіксує це прямо: і `VPIN_BUCKET_VOLUME`, і
   `VPIN_TOXIC_THRESHOLD` свідомо поза сіткою — рухати їх після побаченого OOS було б
   підгонкою. Тому `vpin_momentum` у paper стоїть осторонь, і це задокументований
   результат, а не зламаний режим.
4. **Книга ордерів історично недоступна безкоштовно** (docs/23 §2): у каталозі є
   одна доба ETHUSDT (2026-09-21, знімки 100 мс). Тому `ml_obi` — єдиний робот, чиї
   paper-числа (§3) отримані на добі, а не на півтора року; його OOS не виміряний.

---

## 6. Дефекти, знайдені під час цієї роботи (і виправлені)

Історія тут важливіша за список: **усі вісім знайдені не рев'ю, а прогоном** —
і чотири з них робили цілу гілку коду недосяжною, лишаючись невидимими для тестів.

| # | Дефект | Наслідок | Як виявлено |
|---|---|---|---|
| 1 | `overfit_audit_use_case()` передавав 4 аргументи в `RunOverfitAudit`, який приймав 3 | `lab research --pbo` — `TypeError`; аудит перенавчання недосяжний | тест «усі `*_use_case()` збираються» |
| 2 | `bar_convert` будував `BookLevel(volume=…)`, а поле зветься `size` | будь-який round-trip книги — `TypeError` | round-trip тест книги |
| 3 | `OrderBookDepth10` добиває книгу нульовими рівнями, і вони потрапляли в домен | `MlObiStrategy.on_book()` падав на власному `validate()` | той самий round-trip |
| 4 | 20 рівнів Binance передавались у 10-рівневий контейнер | `ml_obi` не запускався взагалі: `bids length greater than maximum 10` | paper-прогін `ml_obi` |
| 5 | `_has_working_order()` дивився лише на `orders_open`, порожній для INFLIGHT-ордерів | робот бачив `flat=True` при власних незакритих ордерах і накладав входи: **146 ETH проти ~36 ETH ліміту 1x**, −19.65% замість −3.36% | прогони paper-сесій |
| 6 | `to_engine_ticks` передавав `AggTrade.price` (рядок) у `Price`, а `trade_id` — рядком | **будь-який** прогін із тіками падав на першій угоді | перша реальна серія тіків у каталозі |
| 7 | тіки, дрібніші за інкремент 0.001, округлювались у нуль і `TradeTick` їх відкидав | одна така угода валила весь прогін | те саме |
| 8 | `lab ingest` падав посеред години: REST віддає **незакриту** свічку, а домен rightly її відкидає | заливання працювало лише в перші хвилини після межі бару | `lab ingest --start 2026-09-22 --end 2026-09-24` о 12:49 |

Дефекти 6–7 — з однієї функції, і жоден не міг спрацювати, доки в каталозі не
з'явилася жодна серія тіків: шлях існував, був покритий тестами «на папері» й не
виконувався жодного разу. Це найдорожчий клас дефектів у цьому проєкті — і саме
тому живий збір тіків (§5.2.1) має цінність окремо від самих даних.

Дефект 5 — найдорожчий: він ламав сам ризик-шар, а не окрему функцію. Тепер вхід
блокується, поки є inflight-ордер, і це закріплено інтеграційним тестом, який
**падає, якщо захист прибрати** (перевірено: 49 001 проти 98 079 notional на тій
самій фікстурі). Тест, який проходить і з багом, і без нього, не вартий нічого.

Стан гейтів на момент написання: `pytest` **693 passed**, `ruff check` і
`ruff format --check` чисті, `mypy --strict` — 195 файлів без зауважень,
`specs/_validator.py` — 22 спеки, `lab live` — код 1 (fail closed).

---

## 7. Що потрібно, щоб рухатися далі

**Щоб paper став кориснішим:**
1. **Більше історії книги ордерів.** `ml_obi` має одну добу. Збір уперед
   (`lab ingest --depth`) пише по файлу на добу й працює, доки його не зупинять; через
   місяць безперервного збору буде місяць.
2. **Перенавчання `formulaic_lgbm`** на поточному каталозі: зі старою моделлю він
   не торгує взагалі (0 філів у folds=4). `meta_label` уже виміряний (§4).
3. **Калібрування VPIN-кошика під таймфрейм** — єдиний виміряний напрямок, де
   токсичність узагалі перетинає поріг (§5.2.2). Це нова гіпотеза зі своєю спекою
   і підбором на IS, а не правка порога.
4. **Тіки — збирати вперед.** Кожної доби потоку додається ~600 тис. угод; через
   місяць буде місяць, і тоді тік-фільтри можна буде міряти на вибірці, а не на
   20 хвилинах.

**Щоб перейти до live (і чому це досі заборонено):**
- потрібен адаптер виконання з приватними ключами — його свідомо немає;
- головне: **жоден робот не має виміряної переваги над buy&hold** (§4). Виходити в
  реальний ринок із конфігурацією, яка відстає від простого утримання і має
  breakeven нижче сплачених комісій, — це не «наступний крок», а спосіб платити
  комісії швидше. `lab live` лишається fail closed, і це не технічний борг.

**Що свідомо не робилося:**
- пороги `vpin_momentum` не рухалися «щоб заторгувало» (§5.3.3);
- `docs/21` §9.4 — `adaptive_ema` не реанімувався, попри те що тепер проходить
  paper-сесію: `status: rejected` у спеці лишається;
- поріг покриття тестами (`fail_under`) не знижувався.
